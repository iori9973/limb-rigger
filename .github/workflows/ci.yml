name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore Tests~/LimbRigger.Tests.csproj

      - name: Build
        run: dotnet build Tests~/LimbRigger.Tests.csproj --no-restore --configuration Release

      - name: Test
        run: dotnet test Tests~/LimbRigger.Tests.csproj --no-build --configuration Release --logger "console;verbosity=normal"
